{
  "name": "cindi-workspace",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @cindi/client",
    "build": "npm run build --workspace @cindi/client",
    "preview": "npm run preview --workspace @cindi/client"
  }
}
