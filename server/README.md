# Cindi server

This workspace is reserved for Cindi's backend. No server framework, runtime dependencies, API contract, or persistence layer has been selected yet.

When backend work begins, choose these with the product requirements in hand (authentication, data storage, integrations, local-agent communication, deployment target, and security boundaries). Add server source, tests, environment configuration, and scripts here without putting backend code in `client/`.
